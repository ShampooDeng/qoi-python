[project]
name = "qoi-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.4",
]

[tool.basedpyright]
typeCheckingMode = "standard"

[dependency-groups]
dev = [
    "matplotlib>=3.10.9",
]
